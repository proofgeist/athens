import { describe } from 'vitest';

// NOTE: SmartList schema not currently available - tests skipped
describe.skip('SmartList OData API', () => {
  describe('list', () => {
    // All tests skipped - SmartList schema not available
  });
});
